#include "Transport.h"

void Ship::loadGoods() const{
    std::cout << "Loading goods through Ship";
}

void Plane::loadGoods() const{
    std::cout << "Loading goods through Plane";
}

void Truck::loadGoods() const {
    std::cout << "Loading goods through Truck";
}